import { $n as toRef, W as computed, Xn as shallowRef, cr as normalizeStyle, or as normalizeClass, qn as ref, tt as createVNode, vn as watchEffect } from "./vue.runtime.esm-bundler-BUsYEyYy.js";
import { V as propsFactory, d as convertToUnit, n as genericComponent } from "./defineComponent-Zh5gDhh3.js";
import { t as makeComponentProps } from "./component-C9INYTfg.js";
import { t as useRender } from "./useRender-B4oFi6TA.js";
import { t as makeTagProps } from "./tag-Cv4NXV_5.js";
import { t as useBackgroundColor } from "./color-zfCnUIFS.js";
import { i as provideTheme, r as makeThemeProps } from "./theme-BALuYV6k.js";
import { t as useToggleScope } from "./toggleScope-D8cwJ9vA.js";
import { n as useRounded, t as makeRoundedProps } from "./rounded-DUr7q5cA.js";
import { n as useBorder, t as makeBorderProps } from "./border-D1uQG1ey.js";
import { n as useElevation, t as makeElevationProps } from "./elevation-Cor59krV.js";
import { t as useResizeObserver } from "./resizeObserver-CXUGTZPK.js";
import { r as useLayoutItem, t as makeLayoutItemProps } from "./layout-DKWnSP3M.js";
import "/Users/brandonpaterno/Downloads/esp-idf-release-v5.5/examples/protocols/http_server/restful_server/front/web-demo/node_modules/vuetify/lib/components/VFooter/VFooter.css";
//#region node_modules/vuetify/lib/components/VFooter/VFooter.js
var makeVFooterProps = propsFactory({
	app: Boolean,
	color: String,
	height: {
		type: [Number, String],
		default: "auto"
	},
	...makeBorderProps(),
	...makeComponentProps(),
	...makeElevationProps(),
	...makeLayoutItemProps(),
	...makeRoundedProps(),
	...makeTagProps({ tag: "footer" }),
	...makeThemeProps()
}, "VFooter");
var VFooter = genericComponent()({
	name: "VFooter",
	props: makeVFooterProps(),
	setup(props, { slots }) {
		const layoutItemStyles = ref();
		const { themeClasses } = provideTheme(props);
		const { backgroundColorClasses, backgroundColorStyles } = useBackgroundColor(() => props.color);
		const { borderClasses } = useBorder(props);
		const { elevationClasses } = useElevation(props);
		const { roundedClasses } = useRounded(props);
		const autoHeight = shallowRef(32);
		const { resizeRef } = useResizeObserver((entries) => {
			if (!entries.length) return;
			autoHeight.value = entries[0].target.clientHeight;
		});
		const height = computed(() => props.height === "auto" ? autoHeight.value : parseInt(props.height, 10));
		useToggleScope(() => props.app, () => {
			const layout = useLayoutItem({
				id: props.name,
				order: computed(() => parseInt(props.order, 10)),
				position: toRef(() => "bottom"),
				layoutSize: height,
				elementSize: computed(() => props.height === "auto" ? void 0 : height.value),
				active: toRef(() => props.app),
				absolute: toRef(() => props.absolute)
			});
			watchEffect(() => {
				layoutItemStyles.value = layout.layoutItemStyles.value;
			});
		});
		useRender(() => createVNode(props.tag, {
			"ref": resizeRef,
			"class": normalizeClass([
				"v-footer",
				themeClasses.value,
				backgroundColorClasses.value,
				borderClasses.value,
				elevationClasses.value,
				roundedClasses.value,
				props.class
			]),
			"style": normalizeStyle([
				backgroundColorStyles.value,
				props.app ? layoutItemStyles.value : { height: convertToUnit(props.height) },
				props.style
			])
		}, slots));
		return {};
	}
});
//#endregion
export { VFooter };

//# sourceMappingURL=vuetify_components_VFooter.js.map