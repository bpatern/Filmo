import { Hn as onScopeDispose, Nn as effectScope, _n as watch } from "./vue.runtime.esm-bundler-BUsYEyYy.js";
//#region node_modules/vuetify/lib/composables/toggleScope.js
function useToggleScope(source, fn) {
	let scope;
	function start() {
		scope = effectScope();
		scope.run(() => fn.length ? fn(() => {
			scope?.stop();
			start();
		}) : fn());
	}
	watch(source, (active) => {
		if (active && !scope) start();
		else if (!active) {
			scope?.stop();
			scope = void 0;
		}
	}, { immediate: true });
	onScopeDispose(() => {
		scope?.stop();
	});
}
//#endregion
export { useToggleScope as t };

//# sourceMappingURL=toggleScope-D8cwJ9vA.js.map